//      

const path = require('path')

module.exports = function (command        )         {
  return path.join(
    __dirname,
    '..',
    'commands',
    command,
    `${command}-command.js`
  )
}
